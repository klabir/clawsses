# Copy this file to local.properties and restrict it to your user.
# Keep credentials empty for public builds. Private hardware builds may fill both values locally.
# local.properties is ignored by git. Never publish APKs built with production credentials.
rokid.clientSecret=
rokid.accessKey=
